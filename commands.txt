Project Analysis and Server Environment Check
sudo apt update
sudo apt upgrade -y
sudo apt install -y git curl vim

Commands

1. Check system information:
   ssh root@213.210.21.113 "uname -a && cat /etc/os-release"

2. Check Docker and Java versions, and Maven location:
   ssh root@213.210.21.113 "docker --version && java -version && which mvn"

3. Check Docker version, Docker location, Maven location, and Tomcat processes:
   ssh root@213.210.21.113 "docker --version ; which docker ; which mvn ; ps aux | grep -i tomcat"

4. Establish SSH connection:
   ssh root@213.210.21.113

5. Check Docker version after connecting:
   docker --version

Recommendations:
Set Memory Overcommit:
Run the following command to set the memory overcommit:

bash```

echo "vm.overcommit_memory = 1" | sudo tee -a /etc/sysctl.conf
sudo sysctl -p
```
Consider Rootless Mode:
If you're concerned about security, consider setting up Docker in rootless mode using:

bash```

dockerd-rootless-setuptool.sh install
```





$headers = @{ Authorization = "Basic " + [Convert]::ToBase64String([Text.Encoding]::ASCII.GetBytes("s2f08121:FMly9DXV6u!feyZR")) }; Invoke-WebRequest -Uri "https://l8288d7f.ala.asia-southeast1.emqxsl.com:8443/api/v5/clients" -Headers $headers -Method GET

$headers = @{ Authorization = "Basic " + [Convert]::ToBase64String([Text.Encoding]::ASCII.GetBytes("s2f08121:FMly9DXV6u!feyZR")); "Content-Type" = "application/json" }; $body = '{"user_id":"test001","password":"testpass123"}'; Invoke-WebRequest -Uri "https://l8288d7f.ala.asia-southeast1.emqxsl.com:8443/api/v5/authentication/password_based:built_in_database/users" -Headers $headers -Method POST -Body $body

$headers = @{ Authorization = "Basic " + [Convert]::ToBase64String([Text.Encoding]::ASCII.GetBytes("s2f08121:FMly9DXV6u!feyZR")); "Content-Type" = "application/json" }; $body = '{"topic":"device/test001/command","payload":"hello world","qos":1,"retain":false}'; Invoke-WebRequest -Uri "https://l8288d7f.ala.asia-southeast1.emqxsl.com:8443/api/v5/publish" -Headers $headers -Method POST -Body $body

